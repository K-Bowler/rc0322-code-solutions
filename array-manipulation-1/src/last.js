/* exported last */
function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}

// define a new function named last with one parameter (array)
// within the function define a new variable named last index to get the last
// index of the array
// return the array at the lastindex variable to get the last element in the array
