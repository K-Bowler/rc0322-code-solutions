/* exported getLastChar */
function getLastChar(string) {
  var lastIndex = string.length - 1;
  return string[lastIndex];
}

// Define a new function getLastChar with one parameter (string)
// within the function define the last index of the string by subtracting
// 1 from the legnth of the array and assigning that value to the variable
// lastIndex
// return the string at lastIndex
