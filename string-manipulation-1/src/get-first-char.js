/* exported getFirstChar */
function getFirstChar(string) {
  return string[0];
}

// define a new function getFirstChar with one parameter (string)
// within the function return the first index of the string
