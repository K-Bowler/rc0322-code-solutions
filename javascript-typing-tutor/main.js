var $typingText = document.querySelectorAll('span');
var letters =

document.addEventListener('keydown', function (event) {
  for (var i = 0; i < $typingText.length; i++) {
    if (event.target === $typingText[i].classList = 'active') {
      if ()
    }
  }
});
